<%= form_for(@house_hold) do |house_hold| %>

<%= render 'errors' %>

<div class="field">
<%= house_hold.label :name %>
<%= house_hold.text_field :name %>
</div>

<p><%= house_hold.submit "Save" %></p>

<% end %>